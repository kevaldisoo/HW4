import { createStore } from 'vuex'
import posts from './modules/posts'

export default createStore({
    strict: true,
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
      posts,
    }
})
